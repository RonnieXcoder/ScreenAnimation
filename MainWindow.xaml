<Window x:Class="ScreenAnimation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScreenAnimation"
        xmlns:gif="http://wpfanimatedgif.codeplex.com" 
        xmlns:tb="http://www.hardcodet.net/taskbar"  
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400" AllowsTransparency="True" Background="Transparent" Left="0" Top="600" WindowStyle="None" Topmost="True" ShowInTaskbar="False">
    <Grid x:Name="MainGrid">
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Close" Click="CloseMenuItem_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>

        <tb:TaskbarIcon IconSource="icon_16.ico" ToolTipText="hello world">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Add"  Click="AddMenuItem_Click"/>
                    <MenuItem Header="Exit"  Click="ExitMenuItem_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

    </Grid>
    
</Window>
